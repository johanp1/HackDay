component mux4u32sel "mux4u32sel";
pin in unsigned  sel;
pin in float in0;
pin in float in1;
pin in float in2;
pin in float in3;
pin out float out;

function _;
license "GPL"; // indicates GPL v2 or later

;;

if(sel == 0) {out = in0; }
   
if(sel == 1) { out = in1; }
   
if(sel == 2) { out = in2; }
   
if(sel == 3) { out = in3; }
